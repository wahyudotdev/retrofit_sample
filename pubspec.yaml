name: retrofit_sample
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.17.3 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  injectable: ^1.5.3
  json_annotation: ^4.5.0
  retrofit: ^3.0.1+1
  dio: ^4.0.6
  dio_logging_interceptor: ^1.0.1
  get_it: ^7.2.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  build_runner: 
  injectable_generator: 
  json_serializable: 
  retrofit_generator: 

flutter:
  uses-material-design: true
